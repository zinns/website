export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center px-6">
      <section className="w-full max-w-xl text-center">
        <p className="font-mono text-xs uppercase tracking-[0.3em] text-muted">Zinns</p>
        <h1 className="mt-4 text-4xl font-semibold tracking-[-0.06em] sm:text-5xl">
          Blank site baseline
        </h1>
        <p className="mt-4 text-sm leading-7 text-muted sm:text-base">
          Latest packages are in place. Content, layout, color palette, and design direction will be
          defined next.
        </p>
      </section>
    </main>
  );
}
